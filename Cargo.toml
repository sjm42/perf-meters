# Cargo.toml

[package]
name = "perf_meters"
version = "0.1.0"
edition = "2021"


[dependencies]
anyhow = "1.0"
conv = "0.3"
env_logger = "0.10"
log = "0.4"
structopt = "0.3"
sysinfo = "0.26"

[build-dependencies]
build-data = "0.1"


[profile.release]
lto = true
opt-level = 3 #default
# opt-level = 'z'  # Optimize for size.

# EOF
